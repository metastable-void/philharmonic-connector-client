[package]
name = "philharmonic-connector-client"
version = "0.1.2"
edition = "2024"
rust-version = "1.88"
license = "Apache-2.0 OR MPL-2.0"
readme = "README.md"
repository = "https://github.com/metastable-void/philharmonic-connector-client"
description = "Connector-layer lowerer for Philharmonic: decrypts per-tenant endpoint configs, re-encrypts byte-identically to the destination realm's KEM public key, and mints per-step COSE_Sign1 authorization tokens."

[dependencies]
aes-gcm = { version = "0.10", default-features = false, features = ["aes", "alloc"] }
ciborium = { version = "0.2", default-features = false, features = ["std"] }
coset = { version = "0.4", default-features = false, features = [] }
ed25519-dalek = { version = "2", default-features = false, features = ["fast", "std", "zeroize"] }
hkdf = { version = "0.13", default-features = false, features = [] }
ml-kem = { version = "0.2", default-features = false, features = ["deterministic", "std"] }
philharmonic-connector-common = "0.2"
philharmonic-types = "0.3.5"
rand_core = { version = "0.6", default-features = false, features = [] }
secrecy = { version = "0.10", default-features = false, features = [] }
serde = { version = "1", default-features = false, features = ["derive", "std"] }
sha2 = { version = "0.11", default-features = false, features = ["alloc"] }
thiserror = { version = "2", default-features = false, features = ["std"] }
x25519-dalek = { version = "2", default-features = false, features = ["alloc", "precomputed-tables", "static_secrets", "zeroize"] }
zeroize = { version = "1", default-features = false, features = ["alloc", "derive"] }

[dev-dependencies]
hex = { version = "0.4", default-features = false, features = ["std"] }

[profile.release]
opt-level = 3
lto = true
strip = true
codegen-units = 1
panic = "abort"
overflow-checks = true
